// Game of the month voting. Members nominate games any time before the vote, then rank the
// nominations during the monthly meetup so everyone sees the winner together.
export const NOMINATIONS_PER_PERSON = 2;

/** Voting opens this many minutes before the meetup starts. */
export const VOTE_OPENS_BEFORE_START_MIN = 60;
/** And closes this many minutes before the meetup ends, leaving time to reveal the winner. */
export const VOTE_CLOSES_BEFORE_END_MIN = 15;

export const PITCH_MAX = 200;
